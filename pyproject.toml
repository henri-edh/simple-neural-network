[project]
name = "simple-neural-network"
version = "0.1.0"
description = "A single-layer perceptron in Python — learn by watching it train live"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24",
    "matplotlib>=3.7",
]

[dependency-groups]
dev = []
